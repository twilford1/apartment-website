<?php

	//shows info about php and the extensions loaded (like, for example, Xdebug)
	phpinfo();
	
	//if Xdebug is working, this function should return an array
	//echo(json_encode(xdebug_get_code_coverage()));

?>